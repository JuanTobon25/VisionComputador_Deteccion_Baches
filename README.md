# Clasificador de Imágenes con IA 🧠

Esta aplicación web usa un modelo de redes neuronales entrenado con TensorFlow para clasificar imágenes.

## 🚀 Ejecución local
```bash
pip install -r requirements.txt
streamlit run app.py
